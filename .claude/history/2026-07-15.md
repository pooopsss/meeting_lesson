1. You are doing focused research. Do NOT modify any files. Read-only investigation. Project context: Lumen 10 (Laravel micro-framework) on PHP 8.3 inside a Docker container (`php:8.3-fpm-alpine`). Frontend = Vue 3 + PrimeVue 4 (Aura). DB = PostgreSQL 16. Existing code uses Eloquent + facades, tests use `Laravel\Lumen\Testing\DatabaseMigrations` trait, PHPUnit 10 + Mockery. The dev environment runs PHP-FPM behind Nginx, and tests are PHPUnit Feature tests inside the backend container. Goal: produce a TECHNICAL RESEARCH note for a feature "Upload/Download/Delete meeting files" (4 endpoints, 200 MB max file, local disk, Bearer-token auth). The final research doc will be saved at `docs/research-meeting-file-upload.md`. Your job is just to gather facts and write the *Backend* section. Be specific, concrete, and reference exact APIs/methods available in the existing Lumen + Illuminate stack (composer.json declares only `laravel/lumen-framework:^10.0` — NO `illuminate/filesystem` was added e... _(minimax-m3)_
2. You are doing focused research. Do NOT modify any files. Read-only investigation. Project context: - Frontend: Vue 3.5 (Composition API, `<script setup>`) + PrimeVue 4.3 (Aura theme preset) + Vite 6.2 - No router, no Pinia, no Axios (per project CLAUDE.md). Uses native `fetch`. - Existing API client: `frontend/src/api/meetings.js` (uses `fetch` with `Authorization: Bearer <token>` from `frontend/src/store/auth.js` — stores token in `localStorage` via `reactive`/`readonly`) - Existing example of API usage: `frontend/src/components/MeetingsList.vue` (uses `Card`, `Tag`, `Message`, `ProgressSpinner`) - PrimeVue components are NOT auto-imported — must be explicitly imported in `<script setup>` (verified in `MeetingsList.vue:3–6`) - API base URL = `import.meta.env.VITE_API_URL || "http://localhost:8081/api"` - Docker dev server on :5173, mapped to host :5174. Vite hot reload via volume mount Goal: produce the FRONTEND section of a research note about implementing "Upload / List / Downloa... _(minimax-m3)_
3. You are doing focused research. Do NOT modify any files. Read-only investigation. Project context: - Docker Compose with 4 services: postgres, backend (php:8.3-fpm-alpine), nginx (nginx:alpine), frontend (node:20-alpine + Vite) - Both nginx and backend mount `./backend:/var/www` (so PHP-FPM and Nginx see the same `storage/app/` directory) - Nginx config at `nginx/default.conf`: root `/var/www/public`, `try_files $uri $uri/ /index.php?$query_string`, then `location ~ \.php$` with `fastcgi_pass backend:9000` - Backend `Dockerfile` (php:8.3-fpm-alpine): installs `pdo_pgsql`, runs `composer install --no-dev` on every start, chowns `storage` to `www-data` - Backend `docker-entrypoint.sh`: composer install → chown storage → generate APP_KEY → migrate → exec php-fpm - Files for new "meeting files" feature live at `storage/app/meetings/{meeting_id}/{uuid}.{ext}` (per PRD) - Limits: 20 MB for document/image/text/archive, 200 MB for audio/video Goal: produce the INFRASTRUCTURE section of a re... _(minimax-m3)_
4. Необходимо провести ресерч по тому, как оптимальнее реализовать с технологической точки зрения загрузку файлов по плану @docs/plan-meeting-file-upload.md Результат сохрани в отдельный файл в папке docs/research-meeting-file-upload.md _(minimax-m3)_
5. Создай ветку feature/phase-1 и перейди в нее _(minimax-m3)_
6. Бери задачи из milestone Фаза 1: Backend Tracer Bullet — Upload + Download и реализуя эти задачи в этой ветке по мере того, как ты будешь их реализовывать, закрывая задачи, переводя их в готово _(minimax-m3)_
7. бери только с текущего проект _(minimax-m3)_
